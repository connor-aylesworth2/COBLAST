# Development/test dependencies. Runtime dependencies live in requirements.txt.
# Install with: python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8,<9
# Standalone .exe packaging (build_standalone_exe.py).
pyinstaller>=6,<7
